import { FormControl } from '@angular/forms';

export class PlayerNameValidator {
    static differentPlayers(fc: FormControl) {
        
        return (null);
        // return ({differentPlayers: true});
 
    }
}